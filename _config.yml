markdown: GFM

#JustTheDocs
remote_theme: just-the-docs/just-the-docs
title: "Flinch Rules"
description: "Rules of the card game Flinch !"

# different instances of just the docs
collections:
  flinch:
    output: true
    permalink: /flinch/:name/
  #instance2:
   # output: true
    #permalink: /instance2/:name/

# Default settings for collections
defaults:
  - scope:
      path: "_flinch"
    values:
      layout: default
      nav_exclude: false
  #- scope:
   #   path: "_instance2"
   # values:
    #  layout: default
    #  nav_exclude: false

# Just the Docs theme settings
search_enabled: true
heading_anchors: true
